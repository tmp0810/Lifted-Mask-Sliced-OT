# MNIST (planned, not implemented yet)
This folder is reserved for experiment 2. Import the solvers from lmot.
Interpolation must use the coupling, not only its barycentric map. The shared
plan interface provides to_dense() for small measures and apply() for maps.
