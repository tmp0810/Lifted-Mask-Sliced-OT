These are complete example outputs from configs/smoke.yaml using the POT backend.
They are a small functional run, not paper-scale benchmark claims.
Nonconverged rows remain in CSV and are excluded from figures.
Run the experiment again on your own machine for reportable runtime numbers.
