Simulation data are generated from saved configuration and seeds. Each run can
save its exact arrays under results/.../inputs. Future MNIST and image datasets
belong here, but downloaded data are excluded from git.
