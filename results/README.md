Each simulation creates a new run directory containing configuration, environment
metadata, projection banks, optional inputs, raw timings, summary CSV/TSV, and
figures. Checkpoints are written after each dataset. DONE.json means computation
finished. Results are excluded from git; attach selected run artifacts to a
release or archive them separately when reporting experiments.
