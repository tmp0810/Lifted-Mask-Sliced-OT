Archived outputs from the original NumPy/CPU smoke experiment with POT.
These are historical validation data, not results from the current GPU runner.
The old CPU code is available in git history; it is no longer an active backend.
CSV files retain nonconverged rows, while the archived figures exclude them.
Run the CUDA experiment on your own hardware to collect reportable GPU timings.
